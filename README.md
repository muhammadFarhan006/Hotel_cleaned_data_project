# Hotel_cleaned_data_project
Performed end-to-end data cleaning using Python (Pandas & NumPy) to transform raw hotel booking data into an analysis-ready format.
Tasks included handling missing values, removing duplicates, standardizing column names, cleaning categorical text, formatting dates, and validating data consistency.
The final cleaned dataset is ready for analysis and machine learning applications.
